# spring.application.name=managementsystem

# spring.datasource.url=jdbc:mysql://localhost:3306/studentdb
# spring.datasource.username=root
# spring.datasource.password=Abhinav1234

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# server.port=8081

spring.application.name=managementsystem
server.port=${PORT:8081}

# Database Configuration using Environment Variables
# These will be filled by Render's dashboard settings
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:studentdb}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Abhinav1234}

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect